/**
 * Utility methods required in the app
 */

 /**
  * Converts temperature value from celcius to farenheit
  * 
  * @param {number} temp in celcius
  * 
  * @returns {number}
  */
 const celciusToFarenheit = (temp) => {}

/**
 * * Converts temperature value from farenheit to celcius
 * 
 * @param {number} temp in farenheit
 * 
 * @returns {number}
 */
 const farenheitToCelcius = (temp) => {}

/**
 * Converts distance from km to miles
 * 
 * @param {number} value in km
 * 
 * @returns {number}
 */
 const kmToMiles = (value) => {}


 export {
  celciusToFarenheit,
  farenheitToCelcius,
  kmToMiles
 }
